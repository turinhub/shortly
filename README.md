# Shortly

一个基于 Next.js 和 PostgreSQL 的现代化短链接管理平台。让每一个链接更有价值。

## ✨ 特性

- **🔗 链接管理**: 一键生成短链接，支持自定义短码、标签和描述。
- **📊 数据分析**: 实时追踪点击趋势、设备分布和流量来源，全方位了解链接表现。
- **📱 二维码**: 自动生成链接二维码，支持扫码直接跳转。
- **🎨 现代 UI**: 基于 Shadcn UI 和 Tailwind CSS 构建的精美界面，支持明暗模式。
- **🛡️ 安全可靠**: 数据加密存储，基于 PostgreSQL 的稳健后端。

## 🛠️ 技术栈

- **框架**: [Next.js 16](https://nextjs.org/) (App Router)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **样式**: [Tailwind CSS](https://tailwindcss.com/)
- **组件库**: [Shadcn UI](https://ui.shadcn.com/)
- **数据库**: [PostgreSQL](https://www.postgresql.org/)
- **图标**: [Lucide React](https://lucide.dev/)

## 🚀 快速开始

### 前置要求

- Node.js >= 18
- pnpm (推荐) 或 npm/yarn
- PostgreSQL 数据库

### 安装步骤

1. **克隆仓库**

```bash
git clone <repository-url>
cd shortly
```

2. **安装依赖**

```bash
pnpm install
```

3. **配置环境变量**

复制 `.env.example` 文件为 `.env`：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入正确的数据库连接字符串和其他配置：

```env
DATABASE_URL=postgresql://user:password@localhost:5432/shortly
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

4. **数据库初始化**

运行迁移脚本以创建必要的数据库表：

```bash
npx tsx scripts/migrate.ts
```

5. **启动开发服务器**

```bash
pnpm dev
```

打开浏览器访问 [http://localhost:3000](http://localhost:3000) 即可开始使用。

## 📜 常用命令

| 命令 | 说明 |
|------|------|
| `pnpm dev` | 启动开发服务器 |
| `pnpm build` | 构建生产版本 |
| `pnpm start` | 启动生产服务器 |
| `pnpm lint` | 运行代码检查 |
| `pnpm format` | 格式化代码 |

## 📁 项目结构

```
shortly/
├── app/                 # Next.js App Router 页面和 API 路由
├── components/          # React 组件
│   ├── ui/             # Shadcn UI 基础组件
│   └── ...             # 业务组件
├── lib/                 # 工具函数、数据库操作、Server Actions
├── migrations/          # SQL 迁移文件
├── public/              # 静态资源
├── scripts/             # 数据库维护脚本
└── styles/              # 全局样式
```

## TODO

- [ ] 增加登录
- [x] 修复柱状图
- [*] 二维码直接跳转，而非显示链接
